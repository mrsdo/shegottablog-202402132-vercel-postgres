import { roboto } from '@/app/ui/fonts';

export default function M29Logo() {
  return (
    <div
      className={`${roboto.className} flex flex-row items-center leading-none text-white`}
    >
      <p className="text-[44px] font-bold">~M•29</p>
    </div>
  );
}
